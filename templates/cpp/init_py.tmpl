"""{{bundle_name}} - {{description}}"""

from chimerax.core.toolshed import BundleAPI


class _{{pascal_name}}API(BundleAPI):
    """Bundle API for {{bundle_name}}."""

    api_version = 1

    @staticmethod
    def register_command(bundle_info, command_info, logger):
        """Register the {{command_name}} command."""
        from . import cmd
        from chimerax.core.commands import register

        register("{{command_name}}", cmd.{{command_name}}_desc, cmd.{{command_name}}, logger=logger)


bundle_api = _{{pascal_name}}API()
